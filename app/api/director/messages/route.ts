import { NextRequest, NextResponse } from "next/server";
import { askDirector, draftProposal } from "@/lib/director";
import { validateDirectorProposal } from "@/lib/proposals";
import { store } from "@/lib/store";
import { errorResponse, localOnly } from "@/lib/http";
import { retrieveCreativeKnowledge } from "@/lib/knowledge";
export const runtime = "nodejs";
export async function POST(request: NextRequest) { const denied = localOnly(request); if (denied) return denied; try { const body = await request.json(); if (!body.projectId || !body.prompt?.trim()) throw new Error("A project and brief are required."); const db = store(); if (!db.getProject(body.projectId)) throw new Error("Project not found."); const assets = db.listAssets(body.projectId); const manifest = assets.map((asset) => ({ id: asset.id, name: asset.name, mime: asset.mime, width: asset.width, height: asset.height, duration: asset.duration })); const knowledge = await retrieveCreativeKnowledge(body.prompt); const draft = await askDirector(body.prompt, body.projectId, knowledge.evidence, manifest); let proposal; try { proposal = validateDirectorProposal({ ...(draft as object), evidence: knowledge }, { projectId: body.projectId, allowedAssetIds: new Set(assets.map((asset) => asset.id)) }); } catch { proposal = validateDirectorProposal({ ...draftProposal(body.prompt, body.projectId), evidence: knowledge }, { projectId: body.projectId, allowedAssetIds: new Set(assets.map((asset) => asset.id)) }); } return NextResponse.json(db.createProposal(body.projectId, body.prompt, proposal), { status: 201 }); } catch (error) { return errorResponse(error); } }
